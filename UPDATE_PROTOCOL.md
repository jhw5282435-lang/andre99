# 안드레99 갱신 프로토콜

## 사용자 → Claude 표준 명령어

### 1) 단일 종목 갱신
```
안드레99 PLTR 갱신
```
Claude가 다음을 수행:
1. web_search로 최신 데이터 (가격, EPS, 매출 성장 등)
2. v3.0 시나리오 산출 (5단계 + 확률)
3. JSON 블록 출력

### 2) 여러 종목 동시 갱신
```
안드레99 PLTR, TSLA, NVDA 갱신
```

### 3) 신규 종목 추가
```
안드레99 AMZN 추가 (신규)
```
Claude가 시나리오 + 모델 신규 산출

### 4) 분기 어닝 후 종합 갱신
```
안드레99 PLTR Q1 어닝 후 시나리오 재검토
```
시나리오 확률 + 가격 전면 재산출

---

## Claude 응답 표준 형식

Claude는 다음 형식으로 응답해야 함:

````json
{
  "TICKER": {
    "name": "...",
    "sector": "...",
    "price": ...,
    "chg_day": ...,
    "chg_52w": ...,
    "models": [...],
    "fv_avg": ...,
    "indicators": [...],
    "scenarios": [...],
    "expected": ...,
    "peers": {"saas": [...], "sec": [...]},
    "cfm": {...},
    "verdict": {...}
  }
}
````

---

## 사용자 적용 방법

1. Claude의 JSON 블록 복사
2. `data.json` 파일 열기
3. `"stocks": { ... }` 안에 새 종목 추가 (또는 기존 종목 교체)
4. `"_meta": { "last_updated": "YYYY.MM.DD" }` 갱신
5. GitHub Desktop → Commit → Push
6. 1분 후 사이트 자동 반영

---

## 갱신 빈도 권장

| 시점 | 항목 |
|---|---|
| 분기 어닝 직후 | 해당 종목 전면 갱신 (시나리오 재산출) |
| 큰 시장 충격 시 | 5종 모두 시나리오 확률 재배정 |
| 콘텐츠 만드는 날 | 해당 종목 가격만 즉시 갱신 |
| 평상시 | 갱신 불필요 (모델은 분기 단위로 변함) |
